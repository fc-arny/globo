= content_for :head do
  = stylesheet_link_tag :application

= content_for :bottom do
  = javascript_include_tag :application

= content_for :base_content do
  / Logo, menu, user
  .row-fluid.l-header
    .l-container.g-clearfix
      .g-clearfix
        .b-logo
          %h1
            %a{href: "/"}
              %img{src: " /assets/logo.png", alt: "Globomarket"}

        .b-menu.g-clearfix
          %ul.menu-list.g-clearfix
            %li.menu-item.active
              %a.menu-link{href: "/goods"}
                %span.upper-text So, let's start to
                %span.menu-text Choose
                %span.lower-text something to eat
                %span.menu-arrow
            %li.menu-item
              %a.menu-link{href: "/pay"}
                %span.upper-text Now you can
                %span.menu-text Pay
                %span.lower-text for the goods
                %span.menu-arrow
            %li.menu-item
              %a.menu-link{href: "/checkout"}
                %span.upper-text Soon you will
                %span.menu-text Get
                %span.lower-text your goods
                %span.menu-arrow

        = render :partial => 'partials/current_user', :locals => {:user => current_user}
  .row-fluid.l-sub-header{:"ng-contoller" => "GoodsController"}
    .l-container.g-clearfix#search
      = store_list
      .b-goods-search
        .b-search
          .search-wrapper
            %input{type: "text", placeholder: "Product search"}
          .search-button
    .l-container.g-clearfix
      = category_list
    - display_flash_message
  = content_for?(:application_content) ? yield(:application_content) : yield

= render :template => 'layouts/base'
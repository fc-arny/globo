-if current_page.last?
  span.pagination__next.disabled= t('views.common.pagination.next').html_safe
-else
  = link_to t('views.common.pagination.next').html_safe,  url, {rel: 'next', remote: remote, class: 'pagination__next'}
